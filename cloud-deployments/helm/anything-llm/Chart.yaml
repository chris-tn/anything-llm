apiVersion: v2
name: anything-llm
description: Helm chart to deploy AnythingLLM (server + collector)
type: application
version: 0.1.0
appVersion: "render"
dependencies:
  - name: postgresql
    repository: https://charts.bitnami.com/bitnami
    version: ^16.7.24
    condition: postgresql.enabled
